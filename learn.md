`Từ z.object` --> DTO - createZodDTO(tạo ra 1 class DTO)
              --> Type - z.infer<>
`DTO` để phục vụ cho interceptor
`type` để quy định kiểu dữ liệu
```z.object tạo ra cái bản thiêt kế quy định (Schema)```